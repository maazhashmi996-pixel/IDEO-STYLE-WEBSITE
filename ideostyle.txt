<!DOCTYPE html>
<html lang="ur" dir="ltr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<title>Ideo Style — Handloom & Heritage</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,500&family=Jost:wght@300;400;500;600&family=Noto+Nastaliq+Urdu:wght@500;700&display=swap" rel="stylesheet">
<style>
  :root{
    --ivory:#FBF5EA;
    --ivory-dim:#F3EADA;
    --maroon:#7A1F3D;
    --maroon-deep:#5B1530;
    --gold:#B8923F;
    --gold-soft:#D9BD7E;
    --pine:#1F4B3F;
    --ink:#2B231D;
    --ink-soft:#6b5f52;
    --line: rgba(43,35,29,0.14);
    font-size:16px;
    box-sizing:border-box;
    padding-top:env(safe-area-inset-top,0px);
    padding-bottom:env(safe-area-inset-bottom,0px);
  }
  *,*::before,*::after{box-sizing:inherit;}
  html{scroll-behavior:smooth; scroll-padding-top:env(safe-area-inset-top,0px);}
  html,body{height:auto; min-height:100%;}
  body{
    margin:0;
    background:var(--ivory);
    color:var(--ink);
    font-family:'Jost', system-ui, sans-serif;
    font-weight:400;
    overflow-x:hidden;
  }
  @media (prefers-color-scheme: dark){
    :root:not([data-theme="light"]){
      --ivory:#1C1712;
      --ivory-dim:#241D16;
      --ink:#EFE6D8;
      --ink-soft:#B7A990;
      --line: rgba(239,230,216,0.14);
      --gold-soft:#C9A863;
    }
  }
  :root[data-theme="dark"]{
    --ivory:#1C1712;
    --ivory-dim:#241D16;
    --ink:#EFE6D8;
    --ink-soft:#B7A990;
    --line: rgba(239,230,216,0.14);
    --gold-soft:#C9A863;
  }
  h1,h2,h3{
    font-family:'Cormorant Garamond', serif;
    font-weight:500;
    margin:0;
    letter-spacing:0.01em;
  }
  .urdu{
    font-family:'Noto Nastaliq Urdu', serif;
    direction:rtl;
    font-weight:700;
    line-height:1.9;
  }
  a{color:inherit; text-decoration:none;}
  img,svg{max-width:100%; display:block;}
  .wrap{max-width:1180px; margin:0 auto; padding-inline:clamp(20px,4vw,48px);}

  /* ---------- Top bar ---------- */
  .topbar{
    position:sticky; top:0; z-index:40;
    top:env(safe-area-inset-top,0px);
    background:color-mix(in srgb, var(--ivory) 90%, transparent);
    backdrop-filter: blur(10px);
    border-bottom:1px solid var(--line);
  }
  .topbar .wrap{
    display:flex; align-items:center; justify-content:space-between;
    padding-top:16px; padding-bottom:16px;
    gap:20px;
  }
  .brand{
    display:flex; align-items:baseline; gap:10px;
    font-size:1.5rem;
    color:var(--maroon-deep);
  }
  [data-theme="dark"] .brand, @media (prefers-color-scheme:dark){ }
  .brand .mark{font-size:1.3rem; color:var(--gold);}
  nav.links{
    display:flex; gap:clamp(16px,3vw,34px);
    font-size:0.92rem;
    color:var(--ink-soft);
  }
  nav.links a{ padding:4px 2px; border-bottom:1px solid transparent; transition:border-color .2s ease, color .2s ease;}
  nav.links a:hover, nav.links a:focus-visible{ color:var(--ink); border-color:var(--gold);}
  .cart-btn{
    display:flex; align-items:center; gap:8px;
    font-size:0.9rem; color:var(--ink);
    border:1px solid var(--line); border-radius:999px;
    padding:8px 16px;
    background:transparent; cursor:pointer;
    font-family:inherit;
    transition: background .2s ease, border-color .2s ease;
  }
  .cart-btn:hover{ border-color:var(--maroon); background:var(--ivory-dim); }

  @media (max-width:760px){
    nav.links{ display:none; }
  }

  /* ---------- Hero ---------- */
  .hero{
    position:relative;
    padding:clamp(52px,9vw,96px) 0 clamp(40px,6vw,64px);
    border-bottom:1px solid var(--line);
    overflow:hidden;
  }
  .hero .wrap{
    display:grid;
    grid-template-columns: 1.05fr 0.95fr;
    gap:clamp(28px,5vw,60px);
    align-items:center;
  }
  @media (max-width:860px){
    .hero .wrap{ grid-template-columns:1fr; }
  }
  .hero-eyebrow{
    font-size:0.95rem;
    color:var(--gold);
    font-family:'Cormorant Garamond', serif;
    font-style:italic;
    margin-bottom:14px;
  }
  .hero h1{
    font-size:clamp(2.6rem, 5.4vw, 4.3rem);
    line-height:1.06;
    color:var(--maroon-deep);
  }
  .hero h1 em{ font-style:italic; color:var(--pine); }
  .hero p.lede{
    margin-top:22px;
    max-width:44ch;
    font-size:1.08rem;
    line-height:1.75;
    color:var(--ink-soft);
  }
  .hero-actions{ display:flex; gap:16px; margin-top:34px; flex-wrap:wrap; }
  .btn-primary, .btn-ghost{
    display:inline-flex; align-items:center; gap:10px;
    padding:14px 30px;
    font-size:0.95rem;
    border-radius:2px;
    cursor:pointer;
    font-family:'Jost', sans-serif;
  }
  .btn-primary{
    background:var(--maroon-deep); color:var(--ivory);
    border:1px solid var(--maroon-deep);
    transition: background .25s ease, transform .2s ease;
  }
  .btn-primary:hover{ background:var(--maroon); }
  .btn-ghost{
    background:transparent; color:var(--ink);
    border:1px solid var(--line);
    transition: border-color .2s ease, background .2s ease;
  }
  .btn-ghost:hover{ border-color:var(--gold); background:var(--ivory-dim); }

  .hero-art{
    position:relative;
    aspect-ratio: 4/5;
    border:1px solid var(--line);
    background:
      radial-gradient(circle at 30% 20%, color-mix(in srgb, var(--maroon) 18%, transparent), transparent 55%),
      radial-gradient(circle at 75% 75%, color-mix(in srgb, var(--pine) 20%, transparent), transparent 50%),
      var(--ivory-dim);
    overflow:hidden;
  }
  .hero-art svg{ width:100%; height:100%; }

  /* Hero border draw-in, one deliberate motion moment */
  .hero-frame{
    position:absolute; inset:14px;
    border:1px solid var(--gold-soft);
    pointer-events:none;
  }
  .hero-frame path{
    fill:none;
    stroke:var(--gold);
    stroke-width:1.4;
    stroke-dasharray: 900;
    stroke-dashoffset:900;
    animation: draw 2.4s ease-out forwards;
  }
  @keyframes draw{ to{ stroke-dashoffset:0; } }
  @media (prefers-reduced-motion: reduce){
    .hero-frame path{ animation:none; stroke-dashoffset:0; }
  }

  /* ---------- Category strip ---------- */
  .cats{ padding:34px 0; border-bottom:1px solid var(--line); }
  .cats .wrap{
    display:flex; gap:14px; overflow-x:auto; scrollbar-width:none;
    padding-bottom:2px;
  }
  .cats .wrap::-webkit-scrollbar{ display:none; }
  .chip{
    flex:0 0 auto;
    font-size:0.88rem;
    padding:9px 20px;
    border:1px solid var(--line);
    color:var(--ink-soft);
    white-space:nowrap;
    transition: border-color .2s ease, color .2s ease;
  }
  .chip.active, .chip:hover{ border-color:var(--maroon); color:var(--maroon-deep); }

  /* ---------- Section heading ---------- */
  .section{ padding:clamp(48px,7vw,80px) 0; }
  .section-head{
    display:flex; align-items:flex-end; justify-content:space-between;
    gap:20px; margin-bottom:clamp(28px,4vw,44px);
    border-bottom:1px solid var(--line);
    padding-bottom:18px;
    flex-wrap:wrap;
  }
  .section-head h2{ font-size:clamp(1.8rem,3vw,2.4rem); color:var(--maroon-deep); }
  .section-head p{ color:var(--ink-soft); font-size:0.95rem; max-width:38ch; }

  /* ---------- Product grid ---------- */
  .grid{
    display:grid;
    grid-template-columns:repeat(auto-fill, minmax(230px,1fr));
    gap:clamp(20px,3vw,30px);
  }
  .card{
    border:1px solid var(--line);
    background:var(--ivory);
    display:flex; flex-direction:column;
    transition: border-color .25s ease, transform .25s ease;
  }
  .card:hover{ border-color:var(--gold); transform:translateY(-3px); }
  .card-art{
    aspect-ratio: 3/4;
    background:var(--ivory-dim);
    position:relative;
    overflow:hidden;
    border-bottom:1px solid var(--line);
  }
  .card-art svg{ width:100%; height:100%; }
  .tag{
    position:absolute; top:10px; left:10px;
    font-size:0.72rem; letter-spacing:0.02em;
    padding:4px 10px;
    background:var(--maroon-deep); color:var(--ivory);
  }
  .card-body{ padding:16px 18px 20px; display:flex; flex-direction:column; gap:6px; flex:1; }
  .card-body .fabric{ font-size:0.78rem; color:var(--ink-soft); }
  .card-body h3{ font-size:1.2rem; font-weight:500; color:var(--ink); }
  .card-foot{
    margin-top:auto; padding-top:10px;
    display:flex; align-items:center; justify-content:space-between;
  }
  .price{ font-family:'Cormorant Garamond', serif; font-size:1.25rem; color:var(--maroon-deep); }
  .add-btn{
    font-size:0.8rem; padding:8px 14px;
    border:1px solid var(--ink);
    background:transparent; color:var(--ink);
    cursor:pointer; font-family:inherit;
    transition: background .2s ease, color .2s ease, border-color .2s ease;
  }
  .add-btn:hover{ background:var(--ink); color:var(--ivory); }
  .add-btn.added{ border-color:var(--pine); color:var(--pine); }

  /* ---------- Strip banner ---------- */
  .banner{
    background:var(--maroon-deep); color:var(--ivory);
    padding:clamp(40px,6vw,64px) 0;
  }
  .banner .wrap{
    display:flex; align-items:center; justify-content:space-between;
    gap:24px; flex-wrap:wrap;
  }
  .banner h2{ color:var(--ivory); font-size:clamp(1.7rem,3vw,2.3rem); max-width:22ch; }
  .banner p{ color:color-mix(in srgb, var(--ivory) 75%, transparent); margin-top:8px; max-width:40ch; }
  .banner .btn-ghost{ border-color:color-mix(in srgb, var(--ivory) 40%, transparent); color:var(--ivory); }
  .banner .btn-ghost:hover{ border-color:var(--gold); background:color-mix(in srgb, var(--ivory) 8%, transparent); }

  /* ---------- Footer ---------- */
  footer{ padding:clamp(40px,6vw,60px) 0 32px; border-top:1px solid var(--line); }
  .foot-grid{
    display:grid; grid-template-columns:1.4fr 1fr 1fr 1.1fr;
    gap:32px; margin-bottom:40px;
  }
  @media (max-width:760px){ .foot-grid{ grid-template-columns:1fr 1fr; } }
  .foot-grid h4{ font-size:0.82rem; color:var(--ink-soft); margin-bottom:14px; font-weight:500; }
  .foot-grid ul{ list-style:none; margin:0; padding:0; display:flex; flex-direction:column; gap:9px; }
  .foot-grid a{ font-size:0.9rem; color:var(--ink); }
  .foot-grid a:hover{ color:var(--maroon); }
  .foot-bottom{
    display:flex; justify-content:space-between; align-items:center;
    padding-top:20px; border-top:1px solid var(--line);
    font-size:0.8rem; color:var(--ink-soft); flex-wrap:wrap; gap:10px;
  }

  :focus-visible{ outline:2px solid var(--gold); outline-offset:2px; }
</style>
</head>
<body>

<header class="topbar">
  <div class="wrap">
    <div class="brand"><span class="mark" aria-hidden="true">◈</span><span>Ideo Style</span></div>
    <nav class="links">
      <a href="#collection">Collection</a>
      <a href="#lawn">Lawn</a>
      <a href="#bridal">Bridal</a>
      <a href="#about">About</a>
    </nav>
    <button class="cart-btn" id="cartBtn" aria-label="Cart">
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M3 4h2l2.4 12.4a2 2 0 0 0 2 1.6h7.6a2 2 0 0 0 2-1.6L21 8H6"/><circle cx="9" cy="20" r="1.3" fill="currentColor" stroke="none"/><circle cx="17" cy="20" r="1.3" fill="currentColor" stroke="none"/></svg>
      <span id="cartCount">0</span>
    </button>
  </div>
</header>

<section class="hero">
  <div class="wrap">
    <div>
      <div class="hero-eyebrow">Handloom &amp; heritage — new season</div>
      <h1>Threads carried <em>from the karkhana</em><br>to your wardrobe</h1>
      <p class="lede">Hand-block prints, zardozi and phulkari worked by karigars across Multan and Lahore. Lawn for the everyday, silk and chiffon for the occasion.</p>
      <div class="hero-actions">
        <a href="#collection" class="btn-primary">Explore collection</a>
        <a href="#bridal" class="btn-ghost">Bridal edit</a>
      </div>
    </div>
    <div class="hero-art">
      <svg viewBox="0 0 400 500" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <pattern id="motif" width="100" height="100" patternUnits="userSpaceOnUse">
            <path d="M50 20 C70 20 80 40 65 55 C80 60 78 85 55 82 C58 65 40 60 35 75 C15 70 15 40 35 35 C25 20 40 15 50 20 Z" fill="none" stroke="var(--gold-soft)" stroke-width="1"/>
          </pattern>
        </defs>
        <rect width="400" height="500" fill="url(#motif)" opacity="0.55"/>
        <path d="M200 120 C240 120 260 160 230 190 C260 200 255 250 205 245 C210 210 175 200 165 230 C120 220 120 160 165 150 C145 120 175 105 200 120 Z" fill="var(--maroon)" opacity="0.85"/>
        <path d="M200 120 C240 120 260 160 230 190 C260 200 255 250 205 245 C210 210 175 200 165 230 C120 220 120 160 165 150 C145 120 175 105 200 120 Z" fill="none" stroke="var(--gold)" stroke-width="2" transform="translate(0,0)"/>
        <circle cx="200" cy="330" r="55" fill="none" stroke="var(--pine)" stroke-width="1.5"/>
        <circle cx="200" cy="330" r="30" fill="var(--pine)" opacity="0.75"/>
      </svg>
      <div class="hero-frame">
        <svg viewBox="0 0 400 500" preserveAspectRatio="none">
          <path d="M2 2 H398 V498 H2 Z"/>
        </svg>
      </div>
    </div>
  </div>
</section>

<div class="cats">
  <div class="wrap">
    <a class="chip active" href="#collection">All</a>
    <a class="chip" href="#lawn">Lawn</a>
    <a class="chip" href="#chiffon">Chiffon &amp; Silk</a>
    <a class="chip" href="#bridal">Bridal</a>
    <a class="chip" href="#kurti">Kurtis</a>
    <a class="chip" href="#dupatta">Dupattas</a>
  </div>
</div>

<section class="section" id="collection">
  <div class="wrap">
    <div class="section-head">
      <h2>This week's edit</h2>
      <p>New prints in unstitched lawn, plus made-to-order occasion wear.</p>
    </div>
    <div class="grid" id="productGrid"></div>
  </div>
</section>

<section class="banner" id="bridal">
  <div class="wrap">
    <div>
      <h2>Bridal &amp; occasion, made to measure</h2>
      <p>Send your measurements and we tailor the zardozi and gota work to fit — 3 to 4 weeks, nationwide delivery.</p>
    </div>
    <a href="#" class="btn-ghost">Book a fitting</a>
  </div>
</section>

<section class="section" id="about">
  <div class="wrap">
    <div class="section-head">
      <h2>Loved for the finishing</h2>
      <p>Every piece is checked by hand before it leaves the karkhana.</p>
    </div>
    <div class="grid" id="productGrid2"></div>
  </div>
</section>

<footer>
  <div class="wrap">
    <div class="foot-grid">
      <div>
        <div class="brand" style="margin-bottom:10px;"><span class="mark" aria-hidden="true" style="font-size:1.1rem;">◈</span><span style="font-size:1.2rem;">Ideo Style</span></div>
        <p style="color:var(--ink-soft); font-size:0.88rem; max-width:32ch; line-height:1.6;">Ethnic wear woven and printed by karigars in Punjab, delivered across Pakistan.</p>
      </div>
      <div>
        <h4>Shop</h4>
        <ul><li><a href="#lawn">Lawn</a></li><li><a href="#chiffon">Chiffon &amp; Silk</a></li><li><a href="#bridal">Bridal</a></li><li><a href="#kurti">Kurtis</a></li></ul>
      </div>
      <div>
        <h4>Help</h4>
        <ul><li><a href="#">Size guide</a></li><li><a href="#">Order tracking</a></li><li><a href="#">Returns</a></li><li><a href="#">Contact us</a></li></ul>
      </div>
      <div>
        <h4>Visit</h4>
        <ul><li><a href="#">Liberty Market, Lahore</a></li><li><a href="#">+92 300 0000000</a></li><li><a href="#">hello@rangrez.pk</a></li></ul>
      </div>
    </div>
    <div class="foot-bottom">
      <span>© 2026 Ideo Style</span>
      <span>Karachi · Lahore · Islamabad</span>
    </div>
  </div>
</footer>

<script>
(function(){
  // Bust + garment silhouettes: a stylized editorial figure (no face detail), inspired
  // by studio fashion-shoot poses — arch backdrop, draped dupatta, jewelry accents.
  const KURTA_D = "M110,8 C95,8 81,17 66,30 L32,66 C24,80 27,95 38,106 L74,112 L42,292 Q110,314 178,292 L146,112 L182,106 C193,95 196,80 188,66 L154,30 C139,17 125,8 110,8 Z";
  const PANTS_D = "M74,8 L146,8 C170,22 181,58 174,96 L126,96 C132,168 132,240 127,314 L93,314 C98,240 98,168 94,96 L46,96 C39,58 50,22 74,8 Z";
  const DUPATTA_D = "M6,70 C70,18 150,14 214,56 C246,76 268,108 292,150 C250,118 198,100 148,104 C168,140 198,168 236,186 C182,192 128,176 90,140 C78,176 80,214 100,250 C64,222 40,182 34,140 C22,150 12,164 6,180 C0,140 -2,102 6,70 Z";
  const NECK_D = "M78,26 C90,40 130,40 142,26";
  const HEAD_D = "M110,-85 C90,-85 78,-68 78,-48 C78,-33 85,-22 96,-14 C83,-6 70,10 66,30 L154,30 C150,10 137,-6 124,-14 C135,-22 142,-33 142,-48 C142,-68 130,-85 110,-85 Z";
  const DRAPE_D = "M150,35 C185,60 200,120 180,190 C170,230 150,270 165,320 L120,315 C110,270 130,230 138,190 C150,140 130,80 100,50 Z";
  const ARCH_D = "M20,400 L20,150 C20,80 75,35 150,35 C225,35 280,80 280,150 L280,400 Z";

  function garmentSVG(type, c, seed, tag){
    const uid = 'g' + seed;
    const gradId = uid + 'grad', patId = uid + 'pat', jaaliId = uid + 'jaali', glowId = uid + 'glow';

    const defs = `<defs>
      <linearGradient id="${gradId}" x1="0" y1="0" x2="0" y2="1">
        <stop offset="0%" stop-color="${c.light}"/>
        <stop offset="100%" stop-color="${c.base}"/>
      </linearGradient>
      <pattern id="${patId}" width="20" height="20" patternUnits="userSpaceOnUse" patternTransform="rotate(${(seed*17)%45})">
        <path d="M10 3 C13 3 15 6 13 9 C15 11 14 14 11 13 C12 11 8 10 7 12 C4 11 4 7 7 6 C5 4 8 2 10 3 Z" fill="${c.accent}" opacity="0.9"/>
      </pattern>
      <pattern id="${jaaliId}" width="24" height="24" patternUnits="userSpaceOnUse">
        <path d="M12,0 L24,12 L12,24 L0,12 Z" fill="none" stroke="${c.accent}" stroke-width="1" opacity="0.3"/>
      </pattern>
      <radialGradient id="${glowId}" cx="50%" cy="30%" r="55%">
        <stop offset="0%" stop-color="${c.bg}" stop-opacity="0.9"/>
        <stop offset="100%" stop-color="${c.bg}" stop-opacity="0"/>
      </radialGradient>
    </defs>`;

    const backdrop = `<rect width="300" height="400" fill="${c.bg}"/>
      <path d="${ARCH_D}" fill="url(#${jaaliId})"/>
      <ellipse cx="150" cy="120" rx="120" ry="110" fill="url(#${glowId})"/>`;

    function piece(d, transform, hemY, hemH){
      const clip = uid + '-' + transform.replace(/[^a-z0-9]/gi,'');
      return `<g transform="${transform}">
        <clipPath id="${clip}"><path d="${d}"/></clipPath>
        <g clip-path="url(#${clip})">
          <rect x="-20" y="-100" width="260" height="440" fill="url(#${gradId})"/>
          <rect x="-20" y="${hemY}" width="260" height="${hemH}" fill="url(#${patId})" opacity="0.85"/>
          <rect x="-20" y="${hemY-3}" width="260" height="3" fill="${c.accent}"/>
        </g>
        <path d="${d}" fill="none" stroke="${c.stroke}" stroke-width="2.5"/>
      </g>`;
    }

    function pearls(){
      let dots = '';
      for(let i=0;i<5;i++){
        dots += `<circle cx="${88+i*17}" cy="${44+Math.abs(2-i)*3}" r="2.6" fill="#FBF5EA" stroke="${c.stroke}" stroke-width="0.6"/>`;
      }
      return dots;
    }

    let body = '';
    if(type === 'kurta' || type === 'suit'){
      const OUT = 'translate(56,97) scale(0.85)';
      let inner = '';
      inner += piece(KURTA_D, '', 250, 62);
      inner += `<path d="${HEAD_D}" fill="${c.stroke}" opacity="0.88"/>`;
      inner += `<path d="${NECK_D}" fill="none" stroke="${c.accent}" stroke-width="3" stroke-linecap="round"/>`;
      if(type === 'suit'){
        inner += `<g clip-path="none">
          <path d="${DRAPE_D}" fill="url(#${patId})" opacity="0.95"/>
          <path d="${DRAPE_D}" fill="none" stroke="${c.stroke}" stroke-width="2"/>
        </g>`;
      }
      if(tag === 'Bridal') inner += pearls();
      body = `<g transform="${OUT}">${inner}</g>`;
      if(type === 'suit'){
        // ankle hint peeking below the kurta hem
        const fy = 97 + 314*0.85;
        body += `<rect x="128" y="${fy-6}" width="44" height="22" fill="${c.base}" opacity="0.9"/>`;
        body += `<ellipse cx="138" cy="${fy+20}" rx="10" ry="5" fill="${c.stroke}"/>`;
        body += `<ellipse cx="162" cy="${fy+20}" rx="10" ry="5" fill="${c.stroke}"/>`;
      }
    } else if(type === 'pajama'){
      body = piece(PANTS_D, 'translate(40,60) scale(1)', 270, 45);
    } else { // dupatta
      body = `<g transform="translate(4,140) scale(1)">
        <clipPath id="${uid}-dp"><path d="${DUPATTA_D}"/></clipPath>
        <g clip-path="url(#${uid}-dp)">
          <rect x="-20" y="-20" width="340" height="240" fill="url(#${gradId})"/>
          <rect x="-20" y="-20" width="340" height="240" fill="url(#${patId})" opacity="0.7"/>
        </g>
        <path d="${DUPATTA_D}" fill="none" stroke="${c.stroke}" stroke-width="2.5"/>
      </g>`;
    }

    return `<svg viewBox="0 0 300 400" xmlns="http://www.w3.org/2000/svg">
      ${defs}
      ${backdrop}
      ${body}
    </svg>`;
  }

  const products = [
    { name:'Anaar Lawn Suit', fabric:'Shalwar qameez, 3-piece lawn', price:'Rs 4,890', tag:'New', type:'suit',
      color:{ bg:'#F3EADA', light:'#B03A52', base:'#7A1F3D', stroke:'#5B1530', accent:'#D9BD7E' } },
    { name:'Sitara Net Dupatta', fabric:'Gota-edged net dupatta', price:'Rs 2,600', tag:null, type:'dupatta',
      color:{ bg:'#F3EADA', light:'#E7DAC0', base:'#C9A863', stroke:'#8A6D2E', accent:'#7A1F3D' } },
    { name:'Shahi Zardozi Kurta', fabric:'Silk kurta, hand zardozi', price:'Rs 12,400', tag:'Bridal', type:'kurta',
      color:{ bg:'#F3EADA', light:'#8C2140', base:'#5B1530', stroke:'#3E0E20', accent:'#C89B3C' } },
    { name:'Neelambari Pajama Set', fabric:'Cotton kurta &amp; pajama', price:'Rs 3,150', tag:null, type:'pajama',
      color:{ bg:'#F3EADA', light:'#3E5F82', base:'#274158', stroke:'#182B3B', accent:'#D9BD7E' } },
    { name:'Gulbahar Chiffon Suit', fabric:'Shalwar qameez, chiffon', price:'Rs 7,250', tag:null, type:'suit',
      color:{ bg:'#F3EADA', light:'#3D6B54', base:'#1F4B3F', stroke:'#122E26', accent:'#D9BD7E' } },
    { name:'Firozi Silk Kurta', fabric:'Pure silk, hand-dyed', price:'Rs 9,800', tag:'Limited', type:'kurta',
      color:{ bg:'#F3EADA', light:'#2E8B8B', base:'#1D5F5F', stroke:'#123D3D', accent:'#C89B3C' } },
    { name:'Booti Print Lawn', fabric:'Shalwar qameez, 2-piece lawn', price:'Rs 3,450', tag:null, type:'suit',
      color:{ bg:'#F3EADA', light:'#C89B3C', base:'#9C7726', stroke:'#6B511A', accent:'#7A1F3D' } },
    { name:'Meherbaan Bridal Set', fabric:'Shalwar qameez, dabka &amp; gota', price:'Rs 32,000', tag:'Bridal', type:'suit',
      color:{ bg:'#F3EADA', light:'#7A1F3D', base:'#4A0F23', stroke:'#2E0815', accent:'#C89B3C' } },
  ];

  function cardHTML(prod, i){
    return `<article class="card">
      <div class="card-art">
        ${prod.tag ? `<span class="tag">${prod.tag}</span>` : ''}
        ${garmentSVG(prod.type, prod.color, i+1, prod.tag)}
      </div>
      <div class="card-body">
        <span class="fabric">${prod.fabric}</span>
        <h3>${prod.name}</h3>
        <div class="card-foot">
          <span class="price">${prod.price}</span>
          <button class="add-btn" data-name="${prod.name}">Add to cart</button>
        </div>
      </div>
    </article>`;
  }

  const grid1 = document.getElementById('productGrid');
  const grid2 = document.getElementById('productGrid2');
  grid1.innerHTML = products.slice(0,4).map(cardHTML).join('');
  grid2.innerHTML = products.slice(4,8).map((p,i)=>cardHTML(p,i+4)).join('');

  let cartCount = 0;
  const cartCountEl = document.getElementById('cartCount');
  document.querySelectorAll('.add-btn').forEach(btn=>{
    btn.addEventListener('click', ()=>{
      cartCount++;
      cartCountEl.textContent = cartCount;
      btn.textContent = 'Added';
      btn.classList.add('added');
      setTimeout(()=>{ btn.textContent = 'Add to cart'; btn.classList.remove('added'); }, 1400);
    });
  });
})();
</script>

</body>
</html>
